<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                                 https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

  <!-- Unidad de persistencia del proyecto de inventario -->
  <persistence-unit name="inventarioPU" transaction-type="RESOURCE_LOCAL">

    <!-- Única entidad mapeada del dominio -->
    <class>modelo.Producto</class>

    <properties>
      <!-- Conexión a H2 en archivo local (se crea ./data/inventario.mv.db) -->
      <property name="jakarta.persistence.jdbc.driver" value="org.h2.Driver"/>
      <property name="jakarta.persistence.jdbc.url" value="jdbc:h2:file:./data/inventario"/>
      <property name="jakarta.persistence.jdbc.user" value="sa"/>
      <property name="jakarta.persistence.jdbc.password" value=""/>

      <!-- Dialecto SQL de H2 -->
      <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>

      <!-- Esquema efímero para aprender: borra y recrea tablas en cada arranque.
           En producción jamás: usar validate o migraciones. -->
      <property name="hibernate.hbm2ddl.auto" value="drop-and-create"/>

      <!-- Ver el SQL real que genera Hibernate -->
      <property name="hibernate.show_sql" value="true"/>
      <property name="hibernate.format_sql" value="true"/>
    </properties>
  </persistence-unit>
</persistence>
